# Classic double-hung: two equal columns, each top/bottom sash

window rectangle(1.0, 1.5)

frame thickness 0.06*min_side depth 0.08 material painted_wood

regions:
  glass = inset(root, 0.06*min_side)

panes glass:
  @root root
  root split(v=0.5) L R
  L split(h=0.5) L_bot L_top
  R split(h=0.5) R_bot R_top
  L_bot eps
  L_top eps
  R_bot eps
  R_top eps

output:
  muntin_width 0.022*min_side
  muntin_depth 0.035
  glass_thickness 0.01
